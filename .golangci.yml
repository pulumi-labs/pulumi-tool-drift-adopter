# golangci-lint configuration
# See https://golangci-lint.run/usage/configuration/

linters:
  enable:
    # Default linters (enabled automatically):
    # - errcheck
    # - govet
    # - ineffassign
    # - staticcheck (includes gosimple)
    # - unused

    # Additional useful linters:
    - revive        # Fast, configurable, extensible, flexible, and beautiful linter
    - misspell      # Finds commonly misspelled English words
    - gocyclo       # Computes cyclomatic complexity
    - dupl          # Detects code cloning
    - gofmt         # Checks code formatting
    - goimports     # Checks import formatting

linters-settings:
  goimports:
    local-prefixes: github.com/pulumi/pulumi-drift-adoption-tool

  revive:
    rules:
      - name: var-naming
        disabled: true
      - name: exported
        disabled: false
      - name: error-return
        disabled: false
      - name: error-naming
        disabled: false

  gocyclo:
    min-complexity: 15

  dupl:
    threshold: 100

issues:
  exclude-rules:
    # Exclude test files from certain checks
    - path: _test\.go
      linters:
        - errcheck
        - dupl
        - gocyclo

    # Allow fmt.Printf in drift_helpers.go for CloudControl debug logging
    - path: test/e2e/drift_helpers\.go
      text: "use of `fmt.Printf`"
      linters:
        - forbidigo

    # Allow debug logging in CloudControl investigation tests
    - path: test/e2e/cloudcontrol/
      text: "use of `fmt.Printf`"
      linters:
        - forbidigo

run:
  timeout: 5m
  tests: true
  build-tags:
    - unit
    - integration
    - e2e
  skip-dirs:
    - test/e2e/cloudcontrol
